import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Импортируйте стили
import App from './App'; // Импорт главного компонента

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);